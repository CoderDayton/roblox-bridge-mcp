# Roblox Bridge Configuration

# Port for the HTTP bridge server (default: 8081)
ROBLOX_BRIDGE_PORT=8081

# Timeout in milliseconds for command execution (default: 30000)
ROBLOX_TIMEOUT_MS=30000

# Number of retry attempts for failed commands (default: 2)
ROBLOX_RETRIES=2

# Logging level (DEBUG, INFO, WARN, ERROR; default: INFO)
LOG_LEVEL=INFO

# API key for bridge server authentication (auto-generated if not set)
# Copy this key to your Roblox plugin settings
# ROBLOX_API_KEY=your-secure-key-here
